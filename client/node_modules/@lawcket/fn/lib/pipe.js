"use strict";
var isPromise = function (v) {
    return v.then && typeof v.then === 'function';
};
var pipe = function () {
    var fn = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        fn[_i] = arguments[_i];
    }
    return function (initial) { return fn.reduce(function (a, n) {
        return isPromise(n)
            ? n.then(a)
            : n(a);
    }, initial); };
};
module.exports = { pipe: pipe };
